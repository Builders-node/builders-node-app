/**
 * The Builders Node community chat.
 *
 * One constant for every link to it: a wrong handle drops a visitor into
 * somebody else's group, and the landing page now points at it from two places
 * that would otherwise drift apart.
 */
export const TELEGRAM_COMMUNITY_URL = "https://t.me/buildersnodecom";

/** Telegram's own blue, so a button is recognisable before it is read. */
export const TELEGRAM_BLUE = "#229ED9";
